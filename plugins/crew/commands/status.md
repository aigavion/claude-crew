---
description: Show the crew roster, shared task board, and your unread message count
allowed-tools: ["Bash"]
---

Run this command and show the user its output verbatim, then add a one-line summary of what stands out (e.g. who is idle, what's unclaimed):

`node "${CLAUDE_PLUGIN_ROOT}/cli.js" status`
